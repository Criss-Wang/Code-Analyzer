procedure procName {
  while ((x < 9) || (!(x > 10))) {
    read x;
    if ((y == 1) && (x != 0)) then {
      y = x + 100;
    } else {
      x = y - 199 * x;
      while (v == 0) {
		    read v;
    	}
    }
    print y;
  }
  x = y * (3 + t) + 4;
  print x;
}